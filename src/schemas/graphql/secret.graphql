type Secret {
  id: ID!
  title: String!
  user: String
  secret: String!
}

type Query {
  secrets: [Secret!]!
  secret(id: ID!): Secret
}

type Mutation {
  createSecret(title: String!, secret: String!, user: String): Secret
}